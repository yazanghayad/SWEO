import TeamInboxesClient from '@/features/settings/components/team-inboxes-client';

export const metadata = { title: 'Settings: Team Inboxes' };

export default function InboxesPage() {
  return <TeamInboxesClient />;
}
