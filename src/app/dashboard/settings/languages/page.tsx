import MultilingualClient from '@/features/settings/components/multilingual-client';

export const metadata = { title: 'Settings: Multilingual' };

export default function LanguagesPage() {
  return <MultilingualClient />;
}
