import OutboundClient from '@/features/settings/components/outbound-client';

export const metadata = { title: 'Settings: Outbound' };

export default function OutboundPage() {
  return <OutboundClient />;
}
