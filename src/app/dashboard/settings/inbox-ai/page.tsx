import InboxAiClient from '@/features/settings/components/inbox-ai-client';

export const metadata = { title: 'Settings: Inbox AI' };

export default function InboxAiPage() {
  return <InboxAiClient />;
}
