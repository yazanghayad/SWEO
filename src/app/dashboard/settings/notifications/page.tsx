import NotificationsClient from '@/features/settings/components/notifications-client';

export const metadata = { title: 'Settings: Notifications' };

export default function NotificationsPage() {
  return <NotificationsClient />;
}
