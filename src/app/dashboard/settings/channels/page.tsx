import AllChannelsClient from '@/features/settings/components/channels/all-channels-client';

export const metadata = { title: 'Settings: All Channels' };

export default function AllChannelsPage() {
  return <AllChannelsClient />;
}
