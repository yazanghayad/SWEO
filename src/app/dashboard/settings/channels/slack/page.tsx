import SlackChannelClient from '@/features/settings/components/channels/slack-channel-client';

export const metadata = { title: 'Settings: Slack' };

export default function SlackChannelPage() {
  return <SlackChannelClient />;
}
