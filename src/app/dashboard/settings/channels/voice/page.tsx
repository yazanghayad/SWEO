import VoiceChannelClient from '@/features/settings/components/channels/voice-channel-client';

export const metadata = { title: 'Settings: Voice (AI)' };

export default function VoiceChannelPage() {
  return <VoiceChannelClient />;
}
