import InstagramChannelClient from '@/features/settings/components/channels/instagram-channel-client';

export const metadata = { title: 'Settings: Instagram' };

export default function InstagramChannelPage() {
  return <InstagramChannelClient />;
}
