import AiAgentSettingsClient from '@/features/settings/components/ai-agent-settings-client';

export const metadata = { title: 'Settings: AI Agent' };

export default function AiAgentPage() {
  return <AiAgentSettingsClient />;
}
