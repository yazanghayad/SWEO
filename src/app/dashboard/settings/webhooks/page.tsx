import WebhooksClient from '@/features/settings/components/webhooks-client';

export const metadata = { title: 'Settings: Webhooks' };

export default function WebhooksPage() {
  return <WebhooksClient />;
}
