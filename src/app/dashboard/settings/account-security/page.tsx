import AccountSecurityClient from '@/features/settings/components/account-security-client';

export const metadata = { title: 'Settings: Account Security' };

export default function AccountSecurityPage() {
  return <AccountSecurityClient />;
}
