# =================================================================
# Authentication Configuration (Appwrite)
# =================================================================
NEXT_PUBLIC_APPWRITE_ENDPOINT=https://fra.cloud.appwrite.io/v1
NEXT_PUBLIC_APPWRITE_PROJECT=
APPWRITE_PROJECT_NAME=
NEXT_PUBLIC_APPWRITE_DATABASE=
NEXT_PUBLIC_APPWRITE_BUCKET=
APPWRITE_API_KEY=

# Optional: Public base URL for server-side callbacks
# In production, set this to https://app.sweo.se
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Root domain for subdomain routing (e.g. sweo.se → acme.sweo.se)
ROOT_DOMAIN=sweo.se
NEXT_PUBLIC_ROOT_DOMAIN=sweo.se

# =================================================================
# Subdomain Provisioning (Cloudflare + Vercel)
# =================================================================
# Auto-creates DNS + Vercel domain when a customer registers
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ZONE_ID=
VERCEL_API_TOKEN=
VERCEL_PROJECT_ID=

# =================================================================
# Error Tracking Configuration (Sentry)
# =================================================================
# To set up Sentry error tracking:
# 1. Create an account at https://sentry.io
# 2. Create a new project for Next.js
# 3. Follow the setup instructions below

# Step 1: Sentry DSN (Required)
# Found at: Settings > Projects > [Your Project] > Client Keys (DSN)

NEXT_PUBLIC_SENTRY_DSN=    #Example: https://****@****.ingest.sentry.io/****

# Step 2: Organization & Project Details
# Found at: Settings > Organization > General Settings

NEXT_PUBLIC_SENTRY_ORG=  # Example: acme-corp
NEXT_PUBLIC_SENTRY_PROJECT=  # Example: nextjs-dashboard

# Step 3: Sentry Auth Token

# Sentry can automatically provide readable stack traces for errors using source maps, requiring a Sentry auth token.
# More info: https://docs.sentry.io/platforms/javascript/guides/nextjs/manual-setup/#step-4-add-readable-stack-traces-with-source-maps-optional

SENTRY_AUTH_TOKEN=    #Example: sntrys_************************************

# Step 4: Environment Control (Optional)
# Set to 'true' to disable Sentry in development

NEXT_PUBLIC_SENTRY_DISABLED="false"

# =================================================================
# AI / Knowledge Ingestion (Fas 3)
# =================================================================
# OpenAI – used for text embeddings & chat completions
OPENAI_API_KEY=

# --- OR use NVIDIA NIM (OpenAI-compatible) ---
NVIDIA_API_KEY=
NVIDIA_API_URL=https://integrate.api.nvidia.com/v1/chat/completions
# NVIDIA_MODEL=meta/llama-3.1-70b-instruct
# NVIDIA_EMBEDDING_MODEL=nvidia/nv-embedqa-e5-v5

# =================================================================
# MongoDB Atlas – Vector Search (required for embeddings)
# =================================================================
# M10+ cluster required (free-tier M0 does NOT support vector indexes)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/?appName=MyApp
MONGODB_DB_NAME=fin_ai

# =================================================================
# Important Notes:
# =================================================================
# 1. Rename this file to '.env' for local development
# 2. Never commit the actual '.env' file to version control
# 3. Make sure to replace all placeholder values with real ones

# =================================================================
# Upstash Redis – semantic caching & rate limiting
# =================================================================
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# =================================================================
# Inngest – durable background job queue
# =================================================================
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=

# =================================================================
# Encryption – AES-256-GCM for connector credentials
# =================================================================
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=

# =================================================================
# Twilio – SMS, WhatsApp & Voice webhooks (optional)
# =================================================================
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
TWILIO_WHATSAPP_NUMBER=
# Separate SMS number (if different from TWILIO_PHONE_NUMBER)
TWILIO_SMS_NUMBER=
# Phone number to transfer escalated voice calls to (optional)
TWILIO_TRANSFER_NUMBER=

# =================================================================
# Meta (Instagram DM & Facebook Messenger)
# =================================================================
# Page access token with instagram_manage_messages + pages_messaging permissions
META_PAGE_ACCESS_TOKEN=
# App secret for webhook signature verification
META_APP_SECRET=
# Verification token for webhook subscription challenge
META_VERIFY_TOKEN=

# =================================================================
# Slack (Community Channels)
# =================================================================
# Bot token (xoxb-...) with chat:write, channels:history, im:history scopes
SLACK_BOT_TOKEN=
# Signing secret for webhook request verification
SLACK_SIGNING_SECRET=

# =================================================================
# Email Channel (inbound/outbound)
# =================================================================
EMAIL_PROVIDER=mailgun
EMAIL_FROM_ADDRESS=
EMAIL_API_KEY=

# =================================================================
# Chatbot fallback contact info
# =================================================================
CHATBOT_FALLBACK_EMAIL=
CHATBOT_FALLBACK_PHONE=

# =================================================================
# Cron Job Authentication
# =================================================================
# Secret for authenticating cron job endpoints (/api/cron/*, /api/embeddings)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRON_SECRET=

# =================================================================
# Resend – Transactional emails (free: 100/day, no credit card)
# Sign up at https://resend.com → API Keys → Create
# Without this key, verification codes are logged to the console.
# =================================================================
RESEND_API_KEY=
MAIL_FROM_ADDRESS=SWEO <onboarding@resend.dev>

# =================================================================
# Contact form submit email
# =================================================================
SUBMIT_EMAIL=info@optitech-sverige.se

# =================================================================
# Stripe – Subscription billing & payments
# =================================================================
# Secret key: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=
# Publishable key (client-side, if needed for Stripe.js)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
# Webhook signing secret: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=
# Price IDs for each plan (create in Stripe Dashboard → Products)
STRIPE_PRICE_GROWTH=
STRIPE_PRICE_ENTERPRISE=

# =================================================================
# E2E Testing (Playwright)
# =================================================================
# Tenant API key for the test tenant (used by inbox-escalation E2E test)
E2E_TENANT_API_KEY=
# Dashboard agent credentials for inbox verification
E2E_AGENT_EMAIL=
E2E_AGENT_PASSWORD=

# 4. Keep your secret keys private and never share them
